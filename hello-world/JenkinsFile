pipeline {
    agent any
    stages {

        stage('Download sources'){
            steps{
             	checkout scm
            }
        }

       }
	post {
		always {
        	cleanWs()
    	}
	}
}