pipeline {
    agent any
    stages {

        stage('Download sources'){
            steps{
             	checkout scm
            }
        }

        stage("Build") {
            steps {
                sh "cd hello-world && ./gradlew build"
            }
        }

       }
	post {
		always {
        	cleanWs()
    	}
	}
}